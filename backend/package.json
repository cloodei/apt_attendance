{
  "name": "apt_attendance",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 8080 --ssl-keyfile key.pem --ssl-certfile cert.pem",
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8080",
    "install": "uv sync"
  }
}
